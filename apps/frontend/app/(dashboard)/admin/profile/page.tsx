/**
 * Admin Profile Page
 * Current admin's own profile with edit, password change, preferences.
 */
export default function AdminProfilePage() {
  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold tracking-tight">Admin Profile</h1>
      </div>
      <p className="text-muted-foreground">Current admin's own profile with edit, password change, preferences. - Implementation pending.</p>
    </div>
  );
}
