/**
 * Notifications Page
 * Notification inbox with read/unread, filters, preferences.
 */
export default function NotificationsPage() {
  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold tracking-tight">Notifications</h1>
      </div>
      <p className="text-muted-foreground">Notification inbox with read/unread, filters, preferences. - Implementation pending.</p>
    </div>
  );
}
