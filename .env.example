# Configure each app using its own .env.example.
# Backend: copy apps/backend/.env.example to apps/backend/.env
# Frontend: copy apps/frontend/.env.example to apps/frontend/.env.local
# These root values are for local database tooling only.
DATABASE_URL=postgresql://annotator:local_password@localhost:5432/annotator_platform
DATABASE_SSL=false
