-- PostgreSQL Schema Overview
-- Core entities for the Annotator Platform

-- Extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Roles and Permissions (RBAC)
-- users, profiles, roles, permissions, user_roles, role_permissions
-- admins, clients, taskers
-- projects, accounts, timesheets
-- notifications, audit_logs, reports, export_jobs, sessions, oauth_accounts

-- See /database/schemas/ for full table definitions
